#!/usr/bin/env node

"use strict"





const doc = `
Name:
	pdir — partition a folder into several.

Usage:
	pdir [-e | --extension] [-n | --dry-run] [--] DIR...
	pdir (-h | --help | --version)

Description:

Arguments:
	DIR...    The directory to partition.

Options:
	-n, --dry-run    Do everything except move the files.
	-h, --help       Display this documentation.
`





const docopt = require('docopt').docopt
const pdir   = require('pdir/app/pdir')
const args   = docopt(doc)





pdir(args)
